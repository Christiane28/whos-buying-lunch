function whosPaying(names) {

  var names = ["Cate", "Christiane", "Jake"];
  var randomName = names[Math.floor(Math.random() * names.length)];

  return randomName + " " + "is going to buy lunch today!";
}
